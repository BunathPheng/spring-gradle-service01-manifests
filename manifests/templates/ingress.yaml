---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spring-gradle-service01                  # Ingress name
  labels:
    app: spring-gradle-service01
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "spring-gradle-service01.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: spring-gradle-service01               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
